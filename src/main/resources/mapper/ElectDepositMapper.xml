<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.donno.nj.dao.ElectDepositDao">
    <resultMap id="BaseResultMap" type="com.donno.nj.domain.ElectDeposit">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="deposit_type" property="electDepositType" jdbcType="INTEGER" typeHandler="com.donno.nj.domain.EnumKeyTypeHandler"/>
        <result column="quantity" property="quantity" jdbcType="INTEGER"/>
        <result column="amount_receivable" property="amountReceivable" jdbcType="FLOAT"/>
        <result column="actual_amount" property="actualAmount" jdbcType="FLOAT"/>
        <result column="note" property="note" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <association property="userId" column="user_idx" select="com.donno.nj.dao.UserDao.findUserIdById">
        </association>
        <association property="userName" column="user_idx" select="com.donno.nj.dao.UserDao.findUserNameById">
        </association>
        <association property="gasCylinderSpec" column="gyn_spec_idx" select="com.donno.nj.dao.GasCylinderSpecDao.findById">
        </association>
    </resultMap>

    <sql id="TableName">
        t_elect_deposit
    </sql>

    <sql id="QryFromTable">
        t_elect_deposit
    </sql>



    <sql id="QueryColumnList">
       *
    </sql>

    <sql id="InsertColumnList">
       user_idx,gyn_spec_idx,deposit_type,quantity,amount_receivable,actual_amount,note
    </sql>


    <sql id="getListCondition">
        <where>
            <if test="userId != null">
                and t_elect_deposit.user_idx = (SELECT  id from t_user where user_id = #{userId})
            </if>

            <if test="specCode != null">
                and t_elect_deposit.gyn_spec_idx = (SELECT  id from t_gas_cylinder_spec where code = #{specCode})
            </if>

            <if test="electDepositType != null">
                and t_elect_deposit.deposit_type =  #{electDepositType.index} )
            </if>

            <if test="startTime != null">
                and t_advice.create_time >= #{startTime}
            </if>

            <if test="endTime != null">
                and #{endTime}  >= t_advice.create_time
            </if>

        </where>
    </sql>

    <select id="getList" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumnList"/>
        FROM <include refid="QryFromTable"/>
        <include refid="getListCondition"/>

        <if test="orderBy != null">
            <if test="orderBy != ''">
                ORDER BY   ${orderBy}
            </if>
        </if>

        <if test="limit != null" >
            <if test="offset != null" >
                limit #{limit} offset #{offset};
            </if>
        </if>

    </select>

    <select id="count" resultType="Integer">
        SELECT COUNT(*)
        FROM  <include refid="QryFromTable"/>
        <include refid="getListCondition"/>
    </select>


    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="QueryColumnList"/>
        FROM <include refid="QryFromTable"/>
        WHERE
        t_elect_deposit.id = #{id}
    </select>

    <insert id="insert">
        INSERT IGNORE INTO <include refid="TableName"/>(<include refid="InsertColumnList"/>)
        VALUES((select id from t_user where user_id = #{userId} ),
        (select id from t_gas_cylinder_spec where code = #{gasCylinderSpec.code} ),
        #{electDepositType.index},
        #{quantity},#{amountReceivable},#{actualAmount},
        #{note})
    </insert>

    <delete id="delete" parameterType="Integer">
        DELETE FROM <include refid="TableName"/>
        WHERE
        id = #{id}
    </delete>


</mapper>